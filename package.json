{
  "name": "demo",
  "version": "0.0.0",
  "private": "true",
  "main": "server.js",
  "scripts": {
    "test": "mocha tests/* --exit --reporter mocha-junit-reporter --reporter-options mochaFile=test-results/junit.xml",
    "start": "node server.js"
  },
  "license": "MIT",
  "devDependencies": {
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1",
    "supertest": "^6.3.4"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
