const assert = require('assert');

describe('Tests de l\'application', function() {
    
    it('Test simple qui doit passer', function() {
        assert.strictEqual(1 + 1, 2);
    });
    
    it('Test addition', function() {
        assert.ok(true);
    });
});
